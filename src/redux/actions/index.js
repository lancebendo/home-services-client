import {
  REQUEST_SUCCESS,
  REQUEST_FAIL,
  requestSuccess,
  requestFail,
} from './core.commons';

import {
  MOCK_REQUEST,
  mockRequest,
} from './core.mock';

import {
  ADDRESS,
  GET_ADDRESSES,
  UPDATE_ADDRESS,
  SET_ADDRESSES,
  getAddresses,
  updateAddress,
  setAddresses,
} from './feature.address';

import {
  USER,
  GET_USER,
  UPDATE_USER,
  SET_USER,
  getUser,
  updateUser,
  setUser,
} from './feature.user';

export {
  // core.commons
  REQUEST_SUCCESS,
  REQUEST_FAIL,
  requestSuccess,
  requestFail,

  // core.mock
  MOCK_REQUEST,
  mockRequest,

  // feature.address
  ADDRESS,
  GET_ADDRESSES,
  UPDATE_ADDRESS,
  SET_ADDRESSES,
  getAddresses,
  updateAddress,
  setAddresses,

  // feature.user
  USER,
  GET_USER,
  UPDATE_USER,
  SET_USER,
  getUser,
  updateUser,
  setUser,
};
